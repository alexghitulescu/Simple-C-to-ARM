// global variable
//int result;

int double(int a)
{
	return (a * 2);
}

int fib(int a)
{
	if(a < 2)
	{
		return (a);
	}
	else
	{
		return (fib(a - 1) + fib(a - 2));
	}
}

int test(int a, int b, int c, int d, int e, int f)
{
	print("\n%d\n",a);
	print("%d\n",b);
	print("%d\n",c);
	<br>;
	print("%d\n",d);
	print("%d\n",e);
	print("%d\n\n",f);
	print("enter a number: ");
	int x = read();
	
	
	print("x = %d",              x                      );
}

int main () 
{
	<br>;
	//global var asignment
	//result = 15;
	
	//print(result);
	
	//local var
	print("enter a number: ");
	int x;
	x = read();
	print("%d \n",x);
	print ("%d",10);
	// expression test
	print("\"%d\"\n",x + x);
	print("%d",x - x);
	print("%d",x + 5);
	
	// function test 
	
	print("test\n");
	print("%d %d %d %d %d %d %d", 0, 1, 2, 3, 4, 5);
	fib(10);
	test(1,2,3,4,5,6);
}
