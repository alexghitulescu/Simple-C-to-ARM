// global variable
//int result;

int double(int a)
{
	return (a * 2);
}

int rec(int a)
{
	if(a)
	{
		print(a);
		return (rec (a - 1));
	}
	else
	{
		print(a);
		return (-1);
	}
}

int main () 
{
	//global var asignment
	//result = 15;
	
	//print(result);
	
	//local var
	int x;
	x = 15;
	print(x);
	print (10);
	// expression test
	print(x + x);
	print(x - x);
	print(x + 5);
	
	// function test 
	
	print(double(30));
	
	rec(10);
}
